#include "main.h"


/**
* void reset_to_98(int *n) : a function that takes a pointer to an int as parameter and updates the value it points to to 98.
* @*n : a pointer that updates the variable passed to the function
*/

void reset_to_98(int *n)
{
	*n = 98;
}
